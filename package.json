{
  "name": "claw-agent-broker",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for remote ACP task dispatch components used by claw-agent-broker.",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  }
}